name: python-ci-pipeline
on:
  workflow_call:
jobs:
  python-pipeline:
    name: Python Pipeline

    runs-on: ubuntu-latest
    steps:
     - uses: irajatpandey/platform-workflows/.github/actions/common-setup@master
       with:
         python-version: "3.12"
     