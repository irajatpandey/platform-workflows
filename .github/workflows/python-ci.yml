name: python-ci-pipeline
on:
  workflow_call:
jobs:
  python-pipeline:
    name: Python Pipeline

    runs-on: ubuntu-latest
    steps:
     - uses: ./.github/actions/common-setup
       with:
         python-version: "3.12"
    